Entidade: Linha

Schema SQL:
CREATE TABLE IF NOT EXISTS linhas (
    id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome               VARCHAR(80) UNIQUE NOT NULL,
    orixa_regente_id   BIGINT NOT NULL,
    orixa_adjunto_id   BIGINT,
    descricao          TEXT NOT NULL,
    caracteristicas    TEXT,
    cores              VARCHAR(100),
    elementos_trabalho VARCHAR(200),
    numero_ordem       INTEGER,
    ativo              BOOLEAN DEFAULT TRUE,
    data_criacao       TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (orixa_regente_id) REFERENCES orixas(id),
    FOREIGN KEY (orixa_adjunto_id) REFERENCES orixas(id)
);

Dados Iniciais:
INSERT INTO linhas (nome, orixa_regente_id, descricao, caracteristicas, cores, elementos_trabalho, numero_ordem, ativo) VALUES
('Linha de Oxalá', 1, 'Pai de todos os Orixás, linha da paz e sabedoria', 'Trabalham com energia de elevação espiritual, cura mental e equilíbrio', 'Branco, Cristal', 'Água benta, flores brancas, velas brancas', 1, true),
('Linha de Iemanjá', 5, 'Rainha do Mar, linha da maternidade e proteção', 'Trabalham com proteção maternal, limpeza espiritual e renovação', 'Azul claro, Branco, Cristal', 'Água do mar, conchas, estrelas do mar', 2, true),
('Linha de Ogum', 2, 'Guerreiro e Protetor, linha da força e dos caminhos', 'Trabalham com abertura de caminhos, proteção e força de vontade', 'Vermelho, Verde', 'Ferro, espadas, folhas de lança', 3, true),
('Linha de Oxóssi', 3, 'Caçador da Mata, linha da fartura e conhecimento', 'Trabalham com prosperidade, conhecimento da natureza e provisão', 'Verde, Azul', 'Folhas, frutas, arco e flecha', 4, true),
('Linha de Xangô', 4, 'Senhor da Justiça, linha do equilíbrio e liderança', 'Trabalham com justiça, liderança e equilíbrio kármico', 'Marrom, Branco', 'Machado duplo, pedreiras, fogueiras', 5, true),
('Linha de Iansã', 7, 'Senhora dos Ventos, linha da transformação', 'Trabalham com mudanças, transformações e renovação de energias', 'Amarelo, Rosa', 'Espadas, raios, ventania', 6, true),
('Linha de Oxum', 6, 'Senhora do Amor, linha do amor e prosperidade', 'Trabalham com amor, relacionamentos, beleza e abundância material', 'Dourado, Amarelo', 'Espelhos, mel, cachoeiras', 7, true);


Endpoints Necessários:
Apenas listagem de ativos (GET /api/v1/linhas/ativos) -> SIM
CRUD completo -> NÃO
Endpoints customizados -> NÃO

Regras de Negócio:
Uma linha DEVE TER sempre um Orixá regente (obrigatório)
Orixá adjunto é opcional
Linhas com ativo = false NÃO aparecem na listagem
numero_ordem é usado para ordenação na exibição
Nome da linha é único no sistema

Relacionamentos:
DTO, apenas um DTO para transferir todos os dados
JPA: @ManyToOne(fetch = FetchType.LAZY) para os Orixás
Não precisa de relacionamento bidirecional (Orixa não precisa conhecer suas Linhas)
Ordenação: ORDER BY numero_ordem ASC na consulta de ativos
Service deve apenas conter o método que será utilizado pelo Controller
Repository deve apenas conter o método que será utilizado pelo Service

🎯 Implementação:

Seguir guidelines padrão com: Entity → LinhaDTO → LinhaMapper (MapStruct) → LinhaRepository → LinhaService → LinhaController


Checklist de Validação:
✅ Funcionalidade necessária agora? SIM
✅ Implementação simples? SIM
✅ Não quebra arquitetura? NÃO QUEBRA
✅ Apenas consulta? SIM
✅ Usar MapStruct? SIM
✅ Relacionamentos como IDs? SIM
✅ Sem índices específicos? SEM ÍNDICES
✅ Seguir padrão arquitetural? SIM
✅ Testes para todas camadas? SIM
✅ UTF-8 configurado? SIM
✅ Atualizar schema.sql/data.sql? SIM

🚀 Comando Final:

"Implemente a entidade Linha seguindo exatamente as DEVELOPMENT_GUIDELINES.md. Crie todas as camadas (Entity, DTO, Mapper, Repository, Service, Controller) com testes completos, seguindo os padrões estabelecidos."